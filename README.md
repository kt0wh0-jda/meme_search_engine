# Meme Search Engine 🔍

*Поиск по тексту внутри мемов через OCR*

## Текущий статус: ⚙️ В разработке

### Что уже работает:
- [x] Структура проекта готова
- [x] Модули созданы (пустые)
- [x] Реализован OCR
- [x] Работа с БД
- [x] Поиск через Левенштейна
- [x] Тест внутри main
- [ ] Скачивание картинок из Телеграм
- [ ] Интерфейс в телеграм 
### (Возможно будет):
- [ ] Классификация с CV по объектам на картинке 
- [ ] Семантический поиск


### Как запустить:
1. `git clone ...`
2. `pip install -r requirements.txt`
3. `python main.py` — будет запускаться позже

### Что делать дальше (мой план):
1. Улучшить предобработку (сделать на основе количества темного или светлого на картинке)
2. Сделать раздельную обработку на русском и английском
3. Реализовать скачивание из телеграм канала
4. Поработать над внутренней документацией
5. ...

### Архитектура (кратко):
# Meme Search Engine — Архитектура MVP

## Общая структура
Проект разделён на четыре независимых слоя: **domain** (доменные сущности), **processing** (обработка изображений и текста), **storage** (хранение данных) и **search** (поисковые алгоритмы). Telegram и UI исключены на этапе MVP.

## Модули и их ответственность

**domain/** — Содержит бизнес-сущности. Определяет модель мема (`Meme`) с полями: id, путь к изображению, исходный и очищенный текст. Не зависит от других слоёв.

**processing/** — Отвечает за преобразование изображений в текст:
- **ocr.py** — распознавание текста с изображений (OCR)
- **preprocessing.py** — подготовка изображений перед распознаванием (обрезка, улучшение)
- **normalization.py** — очистка и нормализация распознанного текста (нижний регистр, удаление лишних пробелов)

**storage/** — Работа с базой данных:
- **database.py** — настройка подключения к БД и управление сессиями
- **models.py** — описание таблиц в виде ORM-моделей
- **repository.py** — слой доступа к данным (добавление, получение мемов)

**search/** — Реализация поиска по тексту:
- **engine.py** — интерфейс поискового движка
- **levenshtein.py** — алгоритм ранжирования на основе расстояния Левенштейна
- **service.py** — сервис, объединяющий доступ к данным и поиск

**config.py** — Настройки проекта: путь к БД, директория изображений, язык OCR.

**main.py** — Точка входа. Координирует запуск обработки, сохранение и поиск.

## Принцип работы
Изображение → Processing (OCR → нормализация) → Storage (сохранение) → Search (поиск по запросу). Каждый слой изолирован, что позволяет легко заменять компоненты.

**Последнее обновление:** 16.12.2024  
**Текущая задача:** Улучшить препроцессинг
